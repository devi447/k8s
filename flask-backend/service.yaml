apiVersion: v1
kind: Service
metadata:
  name: flask-backend-service
  namespace: workshop
spec:
  selector:
    app: flask-backend
  ports:
    - protocol: TCP
      port: 80         # <-- Exposed to Ingress
      targetPort: 5000 # <-- Flask app inside container
  type: ClusterIP
