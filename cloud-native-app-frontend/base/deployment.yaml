apiVersion: apps/v1
kind: Deployment
metadata:
  name: cloud-native-app-frontend
  labels:
    app: cloud-native-app-frontend
spec:
  replicas: 1

  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 0
      maxUnavailable: 1

  selector:
    matchLabels:
      app: cloud-native-app-frontend

  template:
    metadata:
      labels:
        app: cloud-native-app-frontend
    spec:
      # 🔹 REQUIRED FIX (THIS IS THE KEY)
      securityContext:
        fsGroup: 33   # www-data

      containers:
      - name: cloud-native-app-frontend
        image: devidevops447/cloud-native-app-frontend-beta:latest
        ports:
        - containerPort: 8080

        # Resource limits
        resources:
          requests:
            cpu: 300m
            memory: 512Mi
          limits:
            cpu: 500m
            memory: 1Gi

