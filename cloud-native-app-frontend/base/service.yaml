apiVersion: v1
kind: Service
metadata:
  name: cloud-native-app-frontend
spec:
  selector:
    app: cloud-native-app-frontend
  ports:
  - port: 80            # ✅ Ingress talks on 80
    targetPort: 3000    # ✅ Forward to Next.js
