apiVersion: v1
kind: Service
metadata:
  name: cloud-native-app-frontend
spec:
  selector:
    app: cloud-native-app-frontend
  ports:
  - port: 80          # 👈 Ingress talks here
    targetPort: 3000  # 👈 App runs here

  